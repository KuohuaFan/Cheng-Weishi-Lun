# 成唯識論 · 線上讀本　v1.6

護法等菩薩造・唐 玄奘譯・十卷。單檔自足 HTML，無外部相依。

底本：大正新脩大藏經 T31 no.1585，語料取自 CBETA XML TEI-P5。

## 內容規格

| 項目 | 數值 |
|---|---|
| 論文淨字 | 80,205（不含標點與校注，含卷題、題名、後序題款）|
| 分節 | 22（宗前敬序分 1 ＋ 頌組 20 ＋ 釋結施願分 1） |
| 根本頌 | 世親《唯識三十頌》三十頌，全數對位 |
| 科判 | 六級 100 目（CBETA cb:mulu） |
| 校勘 | 394 則全數隨文落地，對校本 12 種，可點閱異文 |
| 缺字 | CBETA 造字 11 字（1 字無 Unicode，以組字式呈現） |
| 撰述層 | 頌旨語譯 22 節・要義提點 96 條・名相 137 條・對照表 6 種 39 列 |

## 問答面板

全螢幕開啟。左上漢堡選單提供：開新對話、重新命名、歸入專案、刪除此對話、
紀錄清單（可切換「僅收藏」）、專案清單與新增專案。標題列右側為分享與關閉。
訊息採無氣泡的直式排版，回答附朗讀與複製。輸入列左側為語音輸入（Web Speech
Recognition，zh-TW）。

紀錄、收藏、專案存於瀏覽器本機（localStorage），不上傳；瀏覽器不支援時自動
降級為單次工作階段記憶體。

## 分頁

論文｜頌旨語譯｜要義提點｜唯識名相

## 目錄三軸

十卷｜三十頌（附五位標記）｜三分科判

## 部署

純靜態，直接置於 GitHub Pages 即可；若 Jekyll 建置失敗，加入 `.nojekyll`。

AI 問答面板需自架 API 轉送端點：修改 `index.html` 中的 `PROXY` 常數。
GitHub Pages 無法自瀏覽器直接呼叫 Anthropic API（CORS 限制）。

## 建置管線

```
T31n1585.xml ──extract.py──▶ corpus.json ──build.py──▶ index.html
                                              ▲
                              meta.py ────────┤
                              gloss.py ───────┘
```

| 檔案 | 角色 |
|---|---|
| `extract.py` | CBETA XML → `corpus.json`：缺字表、`app` 取 `lem`、校勘錨點標記、頌組分節、覆蓋率 assert |
| `corpus.json` | **中間檔**，擷取層成品：22 節論文本體 + 394 則校勘 + 六級科判。可由 extract.py 重新產生 |
| `meta.py` | 書目・版本・授權・變更紀錄 |
| `gloss.py` | 撰述層：頌旨語譯・要義提點・唯識名相・對照表 |
| `build.py` | 讀入上開三者，加掛 `label / yi / yao / ming / tables` 等欄位，組裝 `index.html` 並執行輸出前驗證 |

`index.html` 已內嵌完整語料，**上線只需 `index.html` 一檔**；其餘檔案供覆核與重製。

重跑：

```bash
curl -O https://raw.githubusercontent.com/cbeta-org/xml-p5/master/T/T31/T31n1585.xml
pip install lxml
python3 extract.py && python3 build.py
```

注意分支是 `master` 而非 `main`。`T31n1585.xml` 不隨本倉庫散布，請自 CBETA 取得。

## 版本

- **v1.0**　全文擷取、三十頌分節、四分頁、三軸目錄、校勘徽章、朗讀、問答面板
- **v1.5**　正文覆蓋補足至 100%（卷題、譯者題名、後序題款），校勘 394 則全數落地；
  新增對照表六種三十九列
- **v1.6**　問答改全螢幕無氣泡版式，左上漢堡選單（紀錄・收藏・專案・增刪），
  分享鍵，語音輸入與逐則朗讀

## 授權

見 NOTICE 與 LICENSE-DATA。

## 語音

- 頁首喇叭圖示：朗讀當前節次（依所在分頁朗讀論文／語譯／要義／名相）
- 回答下方「朗讀」：朗讀單則回答
- 輸入列麥克風：語音轉文字輸入

朗讀採句佇列切分（上限 55 字）與 6 秒 resume 心跳，因應 Chrome 長文截斷；
未安裝中文語音時提示並降級。
